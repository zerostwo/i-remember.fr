{
  "name": "@i-remember/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config ../../vite.config.mjs --host 127.0.0.1",
    "build": "vite build --config ../../vite.config.mjs",
    "test": "node -e \"await import('@i-remember/memory-engine'); console.log('web memory engine ok')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@i-remember/memory-engine": "workspace:*"
  }
}
