{
  "name": "@i-remember/api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "test": "node dist/check.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@i-remember/database": "workspace:*",
    "@i-remember/storage": "workspace:*",
    "@i-remember/types": "workspace:*"
  }
}
