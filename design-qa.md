**Findings**
- [P2] WebGL particle terrain is still not pixel-identical frame-to-frame
  Location: original WebGL canvas in `public/js/main.js`, rendered inside `.base-3d-container`.
  Evidence: `qa/compare-original-static.png`, `qa/compare-original-static.html`, `qa/compare-source-hidden-local-after-strip.png`, `qa/live-compare-five-state-contact-sheet.png`, `qa/current-home-source-local-audit.json`, `qa/current-terms-source-local-audit.json`, `qa/current-upload-methods-source-local-audit.json`, `qa/current-explore-source-local-audit.json`, `qa/current-search-open-source-local-audit.json`, `qa/current-autocomplete-api-contract-audit.json`, `qa/current-recovered-post-snapshot-source-audit.json`, `qa/current-french-accent-search-contract-audit.json`, `qa/preview-french-accent-search-contract-audit.json`, `qa/current-search-pagination-api-contract-audit.json`, `qa/current-recovered-post-pagination-contract-audit.json`, `qa/current-related-post-count-api-contract-audit.json`, `qa/current-instagram-token-callback-contract-audit.json`, `qa/current-dearchived-external-url-contract-audit.json`, `qa/current-main-js-source-fidelity-audit.json`, `qa/current-index-html-source-fidelity-audit.json`, `qa/current-fr-html-source-fidelity-audit.json`, `qa/current-legal-html-source-fidelity-audit.json`, `qa/current-font-assets-contract-audit.json`, `qa/current-audio-format-fallback-contract-audit.json`, `qa/current-url-upload-contract-audit.json`, `qa/current-recovered-post-image-assets-audit.json`, `qa/current-recovered-post-image-routing-audit.json`, and `qa/current-production-server-contract-audit.json` show home/add-options, terms, explore/tutorial, search open, autocomplete/search API contract, recovered target-date post source contract, French accent search contract, search pagination API contract, related-post-count API contract, Instagram token callback contract, dearchived external URL contract, main.js source-fidelity contract, HTML source-fidelity contracts, font asset contract, audio format fallback contract, URL-upload contract, recovered post-image asset/routing contract, standalone production server route/API/upload contract, submitted search, and add/upload using the same original layout, UI sprites, typography, copy, controls, transitions, and WebGL canvas backing size. The latest current-source/current-local post-intro home audit at 1280 x 720 measured zero geometry delta for header, add-options, `tell a memory`, `see all memories`, footer, FRM logo, and 1280 x 720 WebGL canvas backing store. The latest Terms audit measured zero geometry delta for the panel, wrapper, close button, title, description, and scrollbar, with matching custom scroll transforms after a wheel interaction. The latest upload-methods audit measured zero geometry delta for the selection container and Facebook/Instagram/local buttons, with matching copy and hover text semantics. The latest Explore audit measured zero geometry delta for the left nav, map/search/add controls, tutorial layer, tutorial icons/text, header, footer, and WebGL canvas, with matching nav/tutorial copy and a side-by-side contact sheet. The latest search-open audit measured zero geometry delta for the search overlay, placeholder, input, search button, underline, left nav rail, header, footer, and WebGL canvas, with matching search/nav copy and hidden hover-label states. The autocomplete/search API audit verifies the original JS literals and local JSONP shape, while the current clean-frame Wayback API endpoints return 404 Wayback HTML. The recovered-post source audit verifies April 30-or-earlier clean page snapshots expand the backend datasets to 706 English posts and 489 French posts while excluding future May captures. The French accent search audits verify `été`, `fête`, and `école` now fold accents for matching without collapsing to one-letter fragments or overmatching adjacent words in dev and production preview. The search pagination API audit verifies the original `lastId=` flow now returns only older ids across the recovered target-date datasets, does not overlap pages, and returns empty when recovered data is exhausted. The related-post-count audit verifies detail tag highlighting now derives from each selected memory and language dataset instead of a fixed placeholder map, including Unicode French tags. The Instagram callback audit verifies the archived OAuth token callback no longer falls through to the app shell and returns standalone `Redirect` HTML. The dearchived external URL audit verifies recovered Twitter/Facebook/Instagram literals now use original target shapes instead of Wayback wrappers while local interception still keeps the revival self-contained. The main.js source-fidelity audit verifies the local core runtime is identical to the recovered archive after Wayback dewrapping. The HTML source-fidelity audits verify the English shell, French shell, and legal page match the recovered HTML after normalizing only revival-required local bootstrap and self-contained URL differences. The font asset audit verifies the archived CSS font-face surface is complete locally across `.eot`, `.woff`, `.ttf`, and `.svg` formats. The audio format fallback audit verifies the original MP3/OGG runtime branch, records that Wayback has no archived OGG rows, confirms the local derived Vorbis OGG files decode, and confirms dev plus production build serve both OGG files. The URL-upload audit verifies the social-photo `api/upload-image` URL field now creates and serves a real original-path upload file instead of falling straight through to the static fallback id in dev and production preview. The recovered post-image audits verify 199 archive-recovered target-date `thumb.jpg` files exist locally and are served before the documented fallback, while unavailable `resized.jpg` paths still fall back cleanly. The production server audit verifies `npm start` serves the restored shells, APIs, static range responses, recovered assets, artifact isolation, and real upload -> post -> direct memory flow. The live five-state run measured changed-pixel ratios of 7.33% home after 9 s, 4.86% add/upload, 3.07% explore, 3.53% search open, and 9.01% terms. DOM/canvas geometry matched in the checked states; stable central home luminance was local/source 0.9997.
  Impact: The large 2D/React mismatch and the earlier over-bright Retina WebGL mismatch are fixed. Strict pixel identity is still not provable because the original scene is moving and randomized, so the exact terrain ridge pattern naturally changes between visits.
  Fix: Default local runtime now forces the archived app onto the same 1x WebGL backing-store behavior observed in Wayback, adds a conservative colorMap startup delay, and serves fallback post images for timestamp-specific archive assets that are no longer reliable. The remaining visual mismatch is stochastic particle placement, not a stable implementation defect.

- [P3] Current Wayback submitted search is broken, but local revival restores it
  Location: `api/search-posts` Wayback request vs. Vite shim in `vite.config.mjs`.
  Evidence: `qa/browser-source-search-family.png` shows the archived page entering `Unexpected Error`; `qa/browser-local-search-family.png` shows the local selected `family` search rail and filtered particle field. `qa/current-autocomplete-api-contract-audit.json` confirms the original `api/auto-complete-tags/` and `api/search-posts` JS contract, local English/French JSONP responses, and current Wayback 404 HTML responses for the equivalent API URLs.
  Impact: This is an intentional revival difference. Matching the current Wayback failure would make search unusable.
  Fix: Kept the local JSON/JSONP API shims for search, related-tag counts, autocomplete, upload, and post submit.

- [P3] Current Wayback direct-memory capture is incomplete, but local revival now restores the archived route contract
  Location: `DEFAULT_POST` setup in `index.html`; `POST_ID_OFFSET = 1248` in the archived `config` module.
  Evidence: `qa/browser-local-direct-memory-4793.png` and `qa/detail-direct-local-4793-open.png` open the `CLCLC` memory (`today with you in HK`) and keep the URL at `/en/memory/4793`; `qa/detail-direct-source-local-4793.json` and `qa/detail-direct-source-4793-unarchived.png` show the current Wayback direct URL has no `.post-2d` detail state.
  Impact: Direct memory sharing is important to the original site, but the current archive does not provide a reliable source-detail render for this path. Earlier `/en/memory/3545` tests were invalid because `3545` is the internal post id, not the public route id.
  Fix: The local static runtime now maps public route id to internal post id via `routeId - 1248`, matching the archived `post2d` URL-writing logic. `qa/detail-memory-smoke.json` also verifies share URLs, close-to-`/en/`, related-tag hover copy, and tag-click search from the restored detail layer.

- [P3] French root and standalone legal routes were missing from the local revival
  Location: archived root `http://i-remember.fr/`, archived legal route `http://i-remember.fr/legal`, and Vite middleware in `vite.config.mjs`.
  Evidence: `/` and `/fr` now return `LANG = 'fr'` with archived French copy; `/legal` returns the standalone legal page with `css/legal.css`; `/memory/5025` injects French post `3777`; `/en/memory/4793` still injects English post `3545`; French and English search APIs return `language_id` 1 and 2 respectively. Browser evidence in `qa/fr-local-clean-smoke.json` confirms `/fr` reaches `app show` with French copy and no console/page errors, and `qa/fr-local-direct-memory-5025-visible.json` confirms `/memory/5025` opens the `Rozenn` panel.
  Impact: Language switching and the legal deep link are part of the original site surface, so leaving them as English fallback routes made the revival incomplete beyond the `/en` visual path.
  Fix: Added archived `fr.html`, `legal.html`, `public/css/legal.css`, and language-aware Vite routing/API selection. Default-memory data is now extracted from the restored HTML `DEFAULT_POSTS` payloads at startup. Fixed the French direct-memory inline regex so the browser no longer treats `//memory` as a comment.

- [P3] Local-only console noise hid real browser health
  Location: autoplay compatibility in `public/js/revival-runtime.js` and default favicon probing in `vite.config.mjs`.
  Evidence: `qa/playwright-clean-smoke.json` verifies the English home state and Terms click path with no console errors/warnings and no 4xx responses. `qa/mobile-local-clean-smoke.json` verifies the same cleanup on a 390 x 844 mobile viewport.
  Impact: Browser console errors made it harder to distinguish real implementation issues from modern browser policy noise and default favicon requests.
  Fix: Return the handled `HTMLMediaElement.play()` promise, suppress only media autoplay `NotAllowedError` unhandled rejections, and return `204` for `/favicon.ico`.

**Open Questions**
- Timestamp-specific Wayback `uploads/posts/*/thumb.jpg` assets are only partially recoverable. The local middleware now serves 199 recovered target-date archived thumbnails where available, then serves `revival-upload` as a compatibility fallback for unavailable post images instead of returning 404s.
- Browser autoplay policy can still abort the background loop request until user interaction; the source page shows the same kind of behavior. The revival runtime now catches rejected `play()` promises so this does not become an unhandled JavaScript error.
- The in-app Browser can open and screenshot the local page, but its DOM snapshot API currently fails with a missing `incrementalAriaSnapshot` backend method and its dev log still reports autoplay policy messages. System Chrome/Playwright is therefore the authoritative rendered QA path for this project until that browser-control channel is fixed.
- Historical Google Analytics and Facebook SDK scripts are now intercepted locally. The restored runtime keeps `_gaq` and the local `FB` mock available, but production preview no longer requests `google-analytics.com`, `connect.facebook.net`, or Wayback SDK URLs.
- The current Wayback French root capture contains French HTML but redirects to the archived English route via its inline `redirectTo`. The local `/fr` route intentionally restores the archived French state instead of preserving that stale redirect.
- The archived footer code contains an unbound Credits click path. The local runtime restores the Credits panel with the original markup; this is verified by `qa/browser-local-credits.png`.
- The archived Facebook share flow redirects popups to `close.html`; the local revival now serves the same minimal `I-Remember` page with `window.close();` instead of letting that path fall through to the app shell.
- In-app browser file injection may still depend on extension permissions, but system Chrome/Playwright now verifies the original `#local-upload` input end to end.

**Implementation Checklist**
- Replaced the React/2D approximation with the archived `index.html`, `css/main.css`, `js/main.js`, and `js/static/modernizr.js`.
- Added `meta charset="utf-8"` so source text such as `«I remember»` renders correctly.
- Added `public/js/revival-runtime.js` with an opt-in deterministic local QA seed mode and a runtime autoplay compatibility guard that catches rejected `HTMLMediaElement.play()` promises without changing the original audio controls.
- Removed early local-only `data-revival`/`data-language` body markers from the app and legal HTML so body markup matches the archived source shape.
- Added a local-only `favicon.ico` 204 response to avoid browser-default 404 console noise.
- Added `server.mjs` plus `npm start` so the revived route/API middleware runs outside Vite preview and serves built assets with byte-range support.
- Forced the archived runtime to use the Wayback-observed 1x canvas backing-store behavior before `main.js` initializes.
- Added a runtime Credits click compatibility handler for the archived footer's missing binding.
- Added Vite middleware for original backend endpoints: search posts, related post counts, autocomplete tags, image upload, and post submit.
- Added `lastId` pagination handling to the restored `api/search-posts` endpoint so deep default-memory browsing does not duplicate the first 200 archived posts.
- Expanded the restored target-date post datasets from additional clean Wayback `DEFAULT_POSTS` snapshots at or before April 30: 706 English posts and 489 French posts, while excluding later May captures.
- Added accent-insensitive, word-boundary matching for restored French search so `été`, `fête`, and `école` preserve useful French tag behavior without broad substring overmatching.
- Replaced the fixed `api/related-post-count` placeholder map with per-post, per-language related tag calculation for the original detail-panel text highlighting contract.
- Restored the archived `/api/instagram-token-callback` OAuth popup callback route with standalone `Redirect` HTML and `captureInstagramToken` handoff.
- Removed residual Wayback URL wrappers from `public/js/main.js` external social/linkification targets while keeping local runtime interception in place.
- Verified `public/js/main.js` remains source-equivalent to the archived runtime after Wayback dewrapping and URL dearchiving.
- Restored all archived ITC Century font formats referenced by the source CSS, not only modern `.woff` files.
- Restored URL-based `api/upload-image` handling for the original social picker upload path when the request carries a same-site `/uploads/*` image URL.
- Updated `api/upload-image` to parse the original multipart image request, keep the real uploaded image in the current server session, and serve it from the original temporary and posted-image URL shapes.
- Restored offset-based direct memory routes for archived default posts and current-session submitted posts.
- Persisted submitted memories and uploaded image bytes under `.revival-storage/`, which is git-ignored, so a restarted local Vite server can still serve the submitted public memory URL and posted image paths.
- Restored archived French root routes, French default-memory data, language-aware search/autocomplete/related count responses, and the standalone legal route.
- Fixed the French direct-memory inline route regex and verified `/fr` plus `/memory/5025` in a real browser.
- Added default post-image fallback handling for `uploads/posts/<id>/thumb.jpg` and `resized.jpg` so the restored scene no longer accumulates image 404s.
- Recovered 199 real archived target-date `uploads/posts/<id>/thumb.jpg` files from Wayback CDX, stored them under `public/uploads/posts/`, and served them before the default image fallback.
- Added JSONP-level Facebook and Instagram compatibility mocks while preserving the archived picker UI.
- Blocked stale Google Analytics and archived Facebook SDK script insertion in `public/js/revival-runtime.js`, preserving the original queue/mock contracts without external network dependencies.
- Added a conservative `img/colorMap.png` startup delay to keep the intro/preloader cadence closer to the source capture.
- Restored the archived background loop audio via `public/audio/loop.mp3`.
- Added derived Vorbis OGG fallbacks for the original audio branch: `public/audio/add.ogg` and `public/audio/loop.ogg`.
- Kept `revival-upload` local assets so the restored upload/post flow can complete.
- Verified end-to-end local upload with `qa/test-upload-600.jpg`, original adjustment/message/terms/submit steps, confirmation screenshot `qa/local-upload-submit-flow.png`, and direct submitted memory screenshot `qa/local-direct-memory-10250-visible.png`.
- Verified current Wayback clean-frame vs local post-intro home geometry with `qa/current-home-source-local-audit.json`, `qa/current-source-home-post-intro.png`, and `qa/current-local-home-post-intro.png`; core layout geometry and WebGL canvas backing size match, while the local page removes the source's Wayback/Facebook/favicon/thumb 404 console noise.
- Verified current Wayback clean-frame vs local Terms panel behavior with `qa/current-terms-source-local-audit.json` and screenshots `qa/current-source-terms-open.png`, `qa/current-local-terms-open.png`, `qa/current-source-terms-scrolled.png`, `qa/current-local-terms-scrolled.png`, `qa/current-source-terms-closed.png`, and `qa/current-local-terms-closed.png`; title/Article 1 text, open geometry, custom scroll movement, and close-to-home behavior match, while the local page has no console warnings/errors or local 4xx/5xx responses.
- Verified current Wayback clean-frame vs local upload-methods behavior with `qa/current-upload-methods-source-local-audit.json` and screenshots `qa/current-source-upload-methods-open.png`, `qa/current-local-upload-methods-open.png`, `qa/current-source-upload-methods-hover-facebook.png`, `qa/current-local-upload-methods-hover-facebook.png`, `qa/current-source-upload-methods-hover-instagram.png`, `qa/current-local-upload-methods-hover-instagram.png`, `qa/current-source-upload-methods-hover-local.png`, and `qa/current-local-upload-methods-hover-local.png`; default copy, source-specific hover copy, Facebook/Instagram/local button geometry, and hover animation semantics match, while the local page has no console warnings/errors or local 4xx/5xx responses.
- Verified current Wayback clean-frame vs local Explore/tutorial behavior with `qa/current-explore-source-local-audit.json` and screenshots `qa/current-source-explore-open.png`, `qa/current-local-explore-open.png`, and `qa/current-explore-source-local-contact-sheet.png`; the flow `/en/` -> `see all memories` reaches the same left nav and tutorial state, nav/tutorial copy matches, checked geometry delta is zero, both WebGL canvases use a 1280 x 720 backing store, and the local page has no console warnings/errors, page errors, bad responses, or relevant request failures.
- Verified current Wayback clean-frame vs local search-open behavior with `qa/current-search-open-source-local-audit.json` and screenshots `qa/current-source-search-open.png`, `qa/current-local-search-open.png`, and `qa/current-search-open-source-local-contact-sheet.png`; the flow `/en/` -> `see all memories` -> search button reaches the same search overlay, placeholder/input/search button/underline/nav rail geometry, copy matches, hover labels remain hidden in both, checked geometry delta is zero, both WebGL canvases use a 1280 x 720 backing store, and the local page has no console warnings/errors, page errors, bad responses, or relevant request failures.
- Verified the current autocomplete/search API contract with `qa/current-autocomplete-api-contract-audit.json`; original source declares `<base href="/">` and calls `api/auto-complete-tags/` plus `api/search-posts`, local English `fam` returns `family`, local French `fam` returns `fameux`/`famille`, local `family` search returns a renderable archived post, and current Wayback clean-frame API URLs return 404 Wayback HTML. Browser and Chrome extension localhost access were blocked by enterprise policy for this check, so no alternate browser surface was used.
- Verified recovered target-date post source coverage with `qa/current-recovered-post-snapshot-source-audit.json`; April 30-or-earlier clean page snapshots expand the backend datasets to 706 English posts and 489 French posts, while later May captures are excluded to preserve the requested April 30 source moment.
- Verified the French accent search contract with `qa/current-french-accent-search-contract-audit.json` and `qa/preview-french-accent-search-contract-audit.json`; local `été` parses to `ete` and returns 28 matches starting with post `3777`, `fête` returns 2 matches starting with post `3179`, `école` returns 4 matches starting with post `3761`, `maison` returns 6 matches, autocomplete for `ét` includes `ete`, English `family` search still works, and production preview preserves the same behavior.
- Verified the default search pagination API contract with `qa/current-search-pagination-api-contract-audit.json` and `qa/current-recovered-post-pagination-contract-audit.json`; the original source uses `lastId=`, local pages are capped at 200 posts, follow-up pages return only older ids with no overlap across the recovered target-date datasets, local submitted posts are separated from archived ordering, and the API returns empty when the recovered oldest ids are exhausted.
- Verified the related-post-count API contract with `qa/current-related-post-count-api-contract-audit.json`; original source uses `api/related-post-count/` to build `.post-2d-message-tag` spans with `data-tag-posts-count`, local English post `3545` returns `today: 5`, local French post `3777` returns Unicode tags such as `été: 26`, `photo: 21`, and `maison: 5`, submitted post `9004` keeps explicit tags, and missing ids return `{}` with `found: false`.
- Verified the Instagram token callback contract with `qa/current-instagram-token-callback-contract-audit.json`; Wayback has a 2014 `text/html` capture for `/api/instagram-token-callback`, original `main.js` uses that route as the Instagram OAuth `redirect_uri`, and local dev plus production preview `/api/instagram-token-callback` now return standalone `Redirect` HTML with `window.opener.captureInstagramToken(token)`, `window.close()`, and no app-shell fallback.
- Verified the dearchived external URL contract with `qa/current-dearchived-external-url-contract-audit.json`; recovered `main.js` now has no Wayback-wrapped Twitter/Facebook/Instagram URL literals, keeps the original target shapes, and `revival-runtime.js` still intercepts restored Facebook/Instagram endpoints locally.
- Verified the main.js source-fidelity contract with `qa/current-main-js-source-fidelity-audit.json`; after removing Wayback wombat wrapping and dearchiving external URL wrappers, the recovered reference runtime and local `public/js/main.js` have matching 560569-byte text.
- Verified the HTML source-fidelity contracts with `qa/current-index-html-source-fidelity-audit.json`, `qa/current-fr-html-source-fidelity-audit.json`, and `qa/current-legal-html-source-fidelity-audit.json`; English, French, and legal HTML match the recovered source after normalizing only the local bootstrap, self-contained base/OG URL choices, direct-memory lookup, and suppressed stale French redirect.
- Verified the archived font asset contract with `qa/current-font-assets-contract-audit.json`; all 24 unique `/fonts/*` references across `public/css/main.css` and `public/css/legal.css` exist locally, cover six `.eot`, six `.woff`, six `.ttf`, and six `.svg` files, pass file-signature checks, return `200` from the dev server, and are copied byte-for-byte into `dist/fonts`.
- Verified the audio format fallback contract with `qa/current-audio-format-fallback-contract-audit.json`; the original runtime selects MP3 or OGG through Modernizr, Wayback has no archived OGG rows, the local derived Vorbis OGG files decode, dev serves them as `audio/ogg`, and the production build copies them into `dist/audio`.
- Verified the URL-based upload contract with `qa/current-url-upload-contract-audit.json`; multipart `api/upload-image` requests with same-site image `url` values create fresh `revival-*` file ids, serve the image from `uploads/tmp/:fileId/resized.jpg`, and preserve source bytes exactly in dev and production preview.
- Verified recovered post-image assets with `qa/current-recovered-post-image-assets-audit.json` and `qa/current-recovered-post-image-routing-audit.json`; 199 recovered `thumb.jpg` files exist locally, route as real archived JPEGs before the fallback, and are copied into `dist/uploads/posts/` by the production build.
- Verified the standalone production server contract with `qa/current-production-server-contract-audit.json`; `npm start` on `127.0.0.1:8080` serves English/French/legal shells, direct memory injection, search/autocomplete/related APIs, recovered thumbnails before fallback, OGG audio, range-capable static files, `close.html`, favicon 204, project-artifact 404s, and upload -> post -> direct submitted-memory flow.
- Verified clean autoplay/home health with `qa/autoplay-clean-audit.json` and `qa/autoplay-clean-audit.png`; Google Chrome shows the original post-intro controls with no console warnings/errors, page errors, bad responses, or relevant request failures beyond the expected `audio/loop.mp3` autoplay abort.
- Verified invalid local upload handling with `qa/test-upload-1x1.png`, `qa/invalid-upload-error-smoke.json`, and `qa/preview-invalid-upload-error-smoke.json`; dev and production preview show the archived dimension error blocker, do not call `api/upload-image`, do not enter adjustment, and dismiss back to upload methods.
- Verified add-flow internal controls with `qa/add-flow-controls-smoke.json`: adjustment Back returns to upload methods, message Terms link opens/closes Terms over the add flow, the terms checkbox enables/disables Confirm, and message Back returns to adjustment.
- Verified the original post-submit `Thank you` share buttons with `qa/congrats-share-smoke.json`, `qa/congrats-share-buttons.png`, and `qa/congrats-share-direct-memory.png`; Facebook, Twitter, and Google Plus target the newly submitted `/en/memory/{id + 1248}` URL, and Facebook returns to `/close.html`.
- Verified durable submitted memory behavior across a Vite restart with `qa/persistence-submit-before-restart.json`, `qa/persistence-check-after-restart.json`, and `qa/persistence-direct-memory-after-restart.png`.
- Verified clean English browser health and Terms interaction with `qa/playwright-clean-smoke.json`, `qa/playwright-clean-home.png`, and `qa/playwright-clean-terms.png`.
- Verified footer sound/share/donate/language controls with `qa/footer-controls-smoke.json` and screenshots `qa/footer-controls-01-home.png` through `qa/footer-controls-06-after-en-nav.png`; the original delayed language routes resolve to `/?ln=fr` and `/en?ln=en`.
- Verified direct memory detail interactions with `qa/detail-memory-smoke.json` and screenshots `qa/detail-memory-01-open.png` through `qa/detail-memory-05-after-tag-click.png`; `/en/memory/4793` opens `CLCLC`, detail share URLs point back to `/en/memory/4793`, close returns to `/en/`, and `/en/memory/5015` supports `friends` tag hover and tag-click search.
- Verified local-only production preview social flow with `qa/preview-facebook-social-local-only.json` and `qa/preview-facebook-social-local-only.png`; the built preview reaches Facebook album, photo selection, and adjustment using the local `FB` mock with zero external requests and no console warnings/errors.
- Verified local-only production preview Instagram flow with `qa/preview-instagram-social-local-only.json` and `qa/preview-instagram-social-local-only.png`; the built preview reaches Instagram photo selection and adjustment with zero external requests and no console warnings/errors.
- Verified production preview local upload and submitted-memory direct route with `qa/preview-local-upload-submit-flow.json`, `qa/preview-local-upload-submit-flow.png`, and `qa/preview-local-upload-direct-memory.png`; the original `#local-upload` path reaches adjustment, message, terms, confirmation, and `/en/memory/10250` with no external requests and no console warnings/errors.
- Verified production preview add-flow internal controls with `qa/preview-add-flow-controls-smoke.json`; the built preview preserves adjustment Back, message Terms link/close, terms checkbox Confirm enable/disable, and message Back without console warnings/errors or local 4xx/5xx responses.
- Verified production preview post-submit sharing with `qa/preview-congrats-share-smoke.json`, `qa/preview-congrats-share-buttons.png`, and `qa/preview-congrats-share-direct-memory.png`; the built preview preserves the `Thank you` share URLs and direct submitted-memory route without console warnings/errors or local 4xx/5xx responses.
- Verified production preview French and fallback states with `qa/preview-fr-and-no-webgl-smoke.json`; the built preview preserves `/fr` home/Terms/Credits/add-upload and English/French no-WebGL blockers without external requests or console warnings/errors.
- Verified search detail behavior with `qa/search-detail-smoke.json`, `qa/search-hint-click-smoke.json`, and `qa/preview-search-detail-smoke.json`; autocomplete, hint-click filtering, selected search rail, and no-result copy work in dev and production preview without external requests or console warnings/errors.
- Verified the archived share callback with `qa/close-page-share-smoke.json` and `qa/preview-close-page-share-smoke.json`; footer Facebook sharing points `redirect_uri` to `/close.html`, and dev plus production preview serve the original close-page script without falling through to the app shell.
- Verified the archived Facebook social picker flow with real mouse clicks through album selection, photo selection, Download, and the stable adjustment screen in `qa/facebook-social-full-flow-final.json` and `qa/facebook-social-full-flow-final-06-adjustment-final.png`.
- Verified production preview mode after `npm run build`: route/API checks passed for `/en`, `/fr`, `/legal`, `/close.html`, `/en/memory/4793`, `/memory/5025`, `/favicon.ico`, and English/French search APIs; rendered preview smoke passed in `qa/preview-clean-smoke.json`, `qa/preview-clean-home.png`, and `qa/preview-clean-terms.png`.
- Removed duplicated `public/qa/` screenshots and the old duplicated `public/assets/` tree so production builds no longer expose QA artifacts or legacy asset aliases; current `dist/` has no `qa/` or `assets/` directory. Dev HTTP surface now returns `404` for `/qa/`, `/assets/`, `/data/`, and project-internal files while keeping original runtime paths working.
- Verified mobile source/local geometry with `qa/mobile-home-compare.json` and verified post-cleanup mobile local health with `qa/mobile-local-clean-smoke.json`.
- Verified French multi-state local behavior with `qa/fr-local-multistate-smoke.json` and screenshots for home, Terms, Credits, and add/upload; `/fr` stays localized and clean across these states.
- Verified the archived no-WebGL fallback with `qa/no-webgl-local-smoke.json`, `qa/no-webgl-local-en.png`, and `qa/no-webgl-local-fr.png`; both language routes show the original static WebGL-required blocker with no console warnings/errors.
- Removed dead React source files and dependencies.
- Removed the earlier canvas brightness filter so the default render stays on the archived runtime's own visual pipeline.
- Verified `npm run build`.

**Follow-up Polish**
- If exact screenshot parity becomes more important than live-runtime fidelity, add a stronger QA-only capture mode that seeds randomness and fixes the clock/camera frame for both captures.

source visual truth path: `https://web.archive.org/web/20140430235338if_/http://i-remember.fr/en/`
implementation screenshot path: `qa/compare-original-static.png`
viewport: 1512 x 745 Chrome desktop viewport
state: current source/local post-intro home geometry, current source/local Terms open/scroll/close geometry, current source/local upload-methods open/hover geometry, current source/local Explore/tutorial geometry, current source/local search-open geometry, current autocomplete/search API contract, current recovered-post source contract, current French accent search contract, current search pagination API contract, current recovered-post pagination contract, current related-post-count API contract, current Instagram token callback contract, current dearchived external URL contract, current main.js source-fidelity contract, current HTML source-fidelity contracts, current font asset contract, current audio format fallback contract, current URL-upload contract, current recovered post-image asset/routing contract, current standalone production server route/API/upload contract, home/add-options, terms, explore/tutorial, search open/autocomplete/hint-click/no-result/submitted search, add/upload, invalid-upload error blocker, add-flow Back/Terms/checkbox controls, Facebook social picker to adjustment, Instagram social picker to adjustment, footer sound/share/donate/language controls, archived share callback, local credits, direct memory route/share/close/tag search, full local upload submission, post-submit Thank you sharing, persisted submitted memory after server restart, mobile home, French home/Terms/Credits/add-upload/direct memory, no-WebGL fallback, production preview home/terms/social/upload/invalid-upload/add-flow-controls/French/fallback/search/share-callback/post-submit-sharing
full-view comparison evidence: `qa/compare-original-static.png`
focused region comparison evidence: focused review included logo/header, fade meter, center controls, upload selector ring, left navigation, tutorial overlay, search input, footer controls, terms panel, and WebGL terrain in the same comparison artifacts.
patches made since previous QA pass: stripped Wayback wrappers from `main.js` and `modernizr.js`, removed residual Wayback URL wrappers from external social/linkification targets, removed local-only body marker attributes, verified main.js and HTML source equivalence after archive dewrapping, restored the complete archived ITC Century font asset set, restored URL-based social image upload handling, expanded target-date recovered post datasets, added accent-insensitive French search matching, recovered 199 real archived target-date post thumbnails, added derived OGG audio fallbacks for the original Modernizr branch, added standalone `server.mjs` production server with shared revival middleware and range-capable static delivery, added fallback responses for unavailable archived post images, added JSONP mocks for Facebook and Instagram, blocked stale Google Analytics and archived Facebook SDK script requests, added conservative colorMap startup delay, forced 1x WebGL backing store before original runtime init, restored loop audio, restored Credits click, restored offset-based direct memory routes, restored dynamic uploaded-image replay, restored archived French/legal routes, restored archived `close.html` share callback, fixed French direct-memory regex, added autoplay promise guard, added favicon 204 shim, persisted submitted memories and uploads, regenerated browser comparison evidence, verified current source/local post-intro home geometry, current source/local Terms open/scroll/close geometry, current source/local upload-methods open/hover geometry, current source/local Explore/tutorial geometry, current source/local search-open geometry, current autocomplete/search API contract, current recovered-post source contract, current French accent search contract, current search pagination API contract, current recovered-post pagination contract, current related-post-count API contract, current Instagram token callback contract, current dearchived external URL contract, current main.js source-fidelity contract, current HTML source-fidelity contracts, current font asset contract, current audio format fallback contract, current URL-upload contract, current recovered post-image asset/routing contract, current standalone production server route/API/upload contract, clean autoplay/home health, complete local upload submission, invalid-upload error blocker, add-flow internal controls, post-submit Thank you sharing, and restart durability, verified production preview Facebook/Instagram/local-upload/invalid-upload/add-flow-controls/French/fallback/search/share-callback/post-submit-sharing flows without external requests, verified French multi-state behavior, no-WebGL fallback, and detailed search interactions.
current result: layout, interaction, canvas backing size, language routes, fallback mode, production preview social/upload flows, and measured WebGL brightness are close to 1:1; local search, Credits, and social/photo API flows intentionally work where the current Wayback capture is partially broken.
